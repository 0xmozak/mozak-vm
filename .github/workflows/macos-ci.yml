name: Run tests on MacOS

on:
  push:
    branches:
      - release/*
    tags:
      - "*"

  # Triggers the workflow every sunday
  schedule:
    - cron: "0 18 * * 0"

env:
  CARGO_TERM_COLOR: always

jobs:
  cargo-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install rust toolkit
        uses: ./.github/actions/rust

      - name: Run tests
        run: cargo test --locked --all-targets

      - name: Create github issue for failed action
        uses: imjohnbo/issue-bot@v3
        if: ${{ failure() }}
        with:
          assignees: "jdkanani, matthiasgoergens"
          labels: "bug, devtools"
          title: "Tests on MacOS failed"
          body: |
            Failed reference github action job: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            The reference ${{ GITHUB_REF_TYPE }} ${{ GITHUB_REF }} was at commit ${{ GITHUB_SHA }} when its run failed.  More information on the failure can be found at ${{ GITHUB_STEP_SUMMARY }}.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
