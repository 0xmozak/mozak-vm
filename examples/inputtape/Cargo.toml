[package]
edition = "2021"
name = "inputtape"
version = "0.1.0"

[features]
native = []
std = [] # TODO: 😱 why this black magick works?

[dependencies]
mozak-sdk = { path = "../../sdk" }
rkyv = { version = "=0.8.0-alpha.1", default-features = false, features = ["pointer_width_32", "alloc"] }
rkyv_derive = "=0.8.0-alpha.1"

[target.'cfg(not(target_os="mozakvm"))'.dependencies]
mozak-prover-sdk = { path = "../../prover-sdk"}

[target.'cfg(not(target_os="mozakvm"))'.dependencies.hex]
version = "0.4"

[[bin]]
name = "inputtapebin"
path = "main_mozak.rs"

[[bin]]
name = "inputtape-native"
path = "main_native.rs"
required-features = ["native"]

[lib]
name = "inputtape"
path = "core_logic.rs"

# The following is read by `run_examples.py`
[package.metadata.mozak]
example_program_id = "MZK-96d92ca8abef82f6c8091e76a38607d673fa6ce6f234fdfc8c12d75d04f12644"
