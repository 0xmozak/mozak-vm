[package]
edition = "2021"
name = "wallet-mozak-guest"
version = "0.1.0"

[features]
native = []

[dependencies]
guest = { path = "../../guest", features = ["std"] }
mozak-sdk = { path = "../../sdk" }
rkyv = { version = "0.7", default-features = false, features = ["pointer_width_32", "alloc"] }

[target.'cfg(not(target_os="mozakvm"))'.dependencies.hex]
version = "0.4"

[[bin]]
name = "walletbin"
path = "main_mozak.rs"

[[bin]]
name = "wallet-native"
path = "main_native.rs"
required-features = ["native"]

[lib]
name = "wallet"
path = "core_logic.rs"
