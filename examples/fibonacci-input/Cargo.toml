[package]
edition = "2021"
name = "fibonacci-input"
version = "0.1.0"

[dependencies]
guest = { path = "../../guest", default-features = false }

# mozak-system = { path = "../../system" }

[[bin]]
name = "fibonacci-input"
path = "main.rs"

[features]
std = ["guest/std"]
