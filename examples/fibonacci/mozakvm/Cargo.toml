[workspace]
[package]
edition = "2021"
name = "fibonacci-mozakvm"
version = "0.1.0"

[dependencies]
mozak-sdk = { path = "../../../sdk", default-features = false }

[features]
std = ["mozak-sdk/default"]
