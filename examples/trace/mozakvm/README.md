An example to show usage of trace

# To run 
To see the trace, run with "trace" feature

```sh
# inside examples directory
cargo mozakvm-run --features="trace" 
```

NOTE: Don't use trace feature while proving!
