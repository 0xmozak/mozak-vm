[package]
edition = "2021"
name = "merkelize-mozak-guest"
version = "0.1.0"

[dependencies]
guest = { path = "../../guest", features = ["std"] }
mozak-sdk = { path = "../../sdk" }


[[bin]]
name = "merkelize-bin"
path = "main.rs"
