[workspace]
members = ["fibonacci", "min-max", "memory-access", "panic", "poseidon2", "static-mem-access", "sha2", "rkyv-serialization", "stdin"]
resolver = "2"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
