[package]
edition = "2021"
name = "sha2-example-mozak-guest"
version = "0.1.0"

[dependencies]
guest = { path = "../../guest" }
sha2 = { version = "0.10", default-features = false }
hex-literal = "0.2.2"

[[bin]]
name = "sha2-example"
path = "main.rs"
