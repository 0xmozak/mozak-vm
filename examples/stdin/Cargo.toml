[package]
edition = "2021"
name = "stdin-mozak-guest"
version = "0.1.0"

[dependencies]
guest = { path = "../../guest", default-features = false }


[features]
std = ["guest/std"]

[[bin]]
name = "stdin"
path = "main.rs"
