[package]
edition = "2021"
name = "min-max"
version = "0.1"

[dependencies]
mozak-sdk = { path = "../../sdk", default-features = false }

[features]
std = ["mozak-sdk/std"]

[[bin]]
name = "min-max"
path = "main.rs"
